# Mini Mall 微商城 · 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | Mini Mall |
| 文档版本 | v1.0 |
| 更新日期 | 2026-09-19 |
| 技术栈 | SpringBoot 3.x + MyBatis-Plus + MySQL 8 + Vue3 + TypeScript |

---

## 1. 项目概述

### 1.1 背景与目标
Mini Mall 是一个「微型」B2C 电商项目，用于学习与演示**完整的全栈电商闭环**。目标是以最小可行规模，跑通：

> 商品浏览 → 用户注册/登录 → 加入购物车 → 下单 → 模拟支付 → 订单管理 → 后台管理（商品/订单/分类）

### 1.2 产品定位
- 单店、单运营方、轻量演示级商城。
- 不接真实支付渠道（模拟支付）、不做真实物流（后台模拟发货）。

### 1.3 预期成果
用户可以完成从注册到下单支付的完整购物流程；运营方可以在后台维护商品、分类，并处理订单（发货）。

---

## 2. 目标用户

| 角色 | 描述 | 使用入口 |
|---|---|---|
| 消费者 | 浏览商品、注册登录、购物、查订单 | 前台（面向 C 端） |
| 运营/管理员 | 商品与分类维护、订单处理 | 后台（面向管理员） |

---

## 3. 角色与权限

| 角色 | 能力 | 认证方式 |
|---|---|---|
| 前台用户 | 浏览/搜索商品、注册登录、加购、下单、支付、查看个人订单、查看会员信息 | JWT（用户体系，`user` 表） |
| 后台管理员 | 登录后台、分类管理、商品管理、订单管理（发货）、图片上传 | JWT（独立管理员体系，`admin` 表） |

> 前台用户与后台管理员为**两套独立账号体系**，各自独立登录入口与 Token，互不通用。

---

## 4. 前台功能需求

### 4.1 商品浏览
- **列表**：分页展示在售商品（封面图、名称、副标题、价格、销量）。
- **详情**：展示商品主图 + 轮播图、名称、价格、库存、销量、富文本详情。
- **搜索**：按商品名称/副标题关键词检索。
- **分类筛选**：按分类（支持二级分类）筛选商品。

**业务规则：**
- 仅展示 `status = 上架` 的商品。
- 列表接口需支持：分页、关键词、分类 ID 三类参数组合。
- 详情附带轮播图列表，主图排在首位。

### 4.2 用户注册 / 登录
- **注册**：用户名（唯一）+ 密码（≥6 位）+ 昵称（选填）等。
- **登录**：用户名 + 密码，返回 Token。

**业务规则：**
- 密码使用 BCrypt 加盐存储，不落明文。
- 注册成功默认创建 `NORMAL` 会员等级、累计消费 0。
- 登录成功返回 Token 与用户信息（含会员等级、累计消费额）。

### 4.3 购物车
- 加入购物车（同商品合并数量）。
- 修改数量、删除条目、勾选/取消勾选。
- 展示购物车总金额（仅计算勾选项）。

**业务规则：**
- 购物车仅登录用户可用。
- 加入时校验库存，数量不得超过当前库存。

### 4.4 下单与订单管理（模拟支付）
- **下单**：从购物车勾选项生成订单，填写收货人/电话/地址。
- **模拟支付**：点击「支付」将订单置为已支付。
- **取消**：未支付订单可取消，回滚库存。
- **订单查询**：查看我的订单列表与详情。

**业务规则：**
- 订单金额 = 各明细「单价 × 数量」之和；下单时对商品名称/图片/单价做快照。
- 下单在事务内扣减库存，附带 `stock >= 数量` 条件防超卖。
- 订单状态机见 §7.1。

### 4.5 会员等级体系
见 §6。

### 4.6 个人中心
- 展示昵称、会员等级徽章、累计消费额。

---

## 5. 后台功能需求

### 5.1 管理员登录
- 独立登录页，独立管理员账号 + 密码，返回管理员 Token。

### 5.2 分类管理
- 分类的增删改查，支持二级分类（`parent_id`）。

### 5.3 商品管理
- 商品的增删改查、上架/下架。
- 上传商品主图与轮播图。

### 5.4 订单管理
- 订单列表（可按状态筛选）与详情查看。
- 「发货」操作：将已支付订单置为已发货。

---

## 6. 会员等级体系

### 6.1 等级定义
| 等级 | 编码 | 升级条件（累计消费额，元） |
|---|---|---|
| 普通 NORMAL | 1 | 默认（< 10,000） |
| 黄金 GOLD | 2 | ≥ 10,000 |
| 铂金 PLATINUM | 3 | ≥ 50,000 |
| 钻石 DIAMOND | 4 | ≥ 100,000 |

### 6.2 规则
1. 用户注册默认为**普通**会员，累计消费 `total_spent = 0`。
2. **累计消费额** = 用户历史订单中「支付成功」订单的实付金额累计。
3. 支付成功时实时结算累计消费额并重算等级。
4. 等级**只升不降**。
5. 本期等级仅作**身份标识与展示**；折扣等会员权益作为扩展点，暂不实现。

---

## 7. 业务规则汇总

### 7.1 订单状态机
```
UNPAID 待支付 ──支付──▶ PAID 已支付 ──发货──▶ SHIPPED 已发货 ──确认──▶ COMPLETED 已完成
     │
     └──取消──▶ CANCELLED 已取消（回滚库存）
```
- 仅 `UNPAID` 可支付、可取消。
- 仅 `PAID` 可发货。

### 7.2 库存与金额
- 下单扣减库存、取消回滚库存；使用条件 UPDATE 防止超卖。
- 金额统一 `DECIMAL(10,2)`，单位元。

---

## 8. 数据需求概述

8 张表：`user`、`admin`、`category`、`product`、`product_image`、`cart_item`、`order`、`order_item`。详见架构设计文档（`plans/ethereal-discovering-toast.md` 或后续 `docs/`）。

关键点：
- 订单与订单明细对商品信息做**冗余快照**，商品后续改价不影响历史订单。
- `user` 表含 `total_spent`（累计消费）与 `member_level`（会员等级）。

---

## 9. 非功能需求

| 维度 | 要求 |
|---|---|
| 安全 | 密码 BCrypt 加密；JWT 鉴权；前台/后台权限隔离；文件上传校验类型与大小 |
| 可维护性 | 分层清晰（controller/service/mapper），DTO 与实体分离，统一返回体 |
| 可用性 | 统一异常处理，友好错误提示 |
| 兼容性 | 现代浏览器（Chrome/Edge 最新版） |

---

## 10. 验收标准

1. 新用户可注册并登录；密码为非明文存储。
2. 首页可检索、可分类筛选、可查看商品详情。
3. 登录用户可将商品加入购物车、改数量、勾选、结算。
4. 可从购物车下单，库存正确扣减；库存不足时下单失败且无超卖。
5. 未支付订单可模拟支付；支付成功后累计消费并触发会员等级升级；可取消未支付订单且库存回滚。
6. 管理员可登录后台，完成分类/商品 CRUD 与图片上传。
7. 管理员可将「已支付」订单发货。
8. 未登录访问需鉴权接口返回 401；普通用户访问后台接口被拒绝。